<h1>タスクリスト一覧</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.id, task %> : <%= task.content %>  sutatus&gt;<%= task.status %></li>
  <% end %>
</ul>

<%= paginate @tasks %>

<%= link_to '新規タスクリストの投稿', new_task_path %>